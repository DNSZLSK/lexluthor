// English verbs : code token -> BASE form (used after "We …" : "We load the grammar").
// Valence and pattern come from the shared (FR-derived) registry ; EN only supplies the
// word. Same KEYS as FR.
import type { VerbEntry } from '../types';

const w = (word: string): VerbEntry => ({ word, valence: 'def' });

export const VERBS_EN: Readonly<Record<string, VerbEntry>> = {
  get: w('get'),
  fetch: w('get'),
  read: w('read'),
  load: w('load'),
  write: w('write'),
  save: w('save'),
  store: w('store'),
  persist: w('store'),
  create: w('create'),
  make: w('create'),
  build: w('build'),
  init: w('initialize'),
  ensure: w('ensure'),
  set: w('set'),
  update: w('update'),
  find: w('find'),
  search: w('search'),
  handle: w('handle'),
  validate: w('validate'),
  check: w('check'),
  verify: w('verify'),
  parse: w('parse'),
  send: w('send'),
  post: w('send'),
  emit: w('emit'),
  delete: w('delete'),
  remove: w('remove'),
  clear: w('clear'),
  compute: w('compute'),
  calculate: w('compute'),
  format: w('format'),
  render: w('render'),
  draw: w('draw'),
  show: w('show'),
  resolve: w('resolve'),
  reject: w('reject'),
  toggle: w('toggle'),
  add: w('add'),
  append: w('add'),
  open: w('open'),
  close: w('close'),
  start: w('start'),
  stop: w('stop'),
  run: w('run'),
  exec: w('run'),
  execute: w('run'),
  apply: w('apply'),
  transform: w('transform'),
  filter: w('filter'),
  sort: w('sort'),
  merge: w('merge'),
  copy: w('copy'),
  move: w('move'),
  normalize: w('normalize'),
  generate: w('generate'),
  extract: w('extract'),
  setup: w('set up'),
  reset: w('reset'),
  collect: w('collect'),
  classify: w('classify'),
  analyze: w('analyze'),
  dispatch: w('dispatch'),
  register: w('register'),
  convert: w('convert'),
  wrap: w('wrap'),
  unwrap: w('unwrap'),
  accept: w('accept'),
  escape: w('escape'),
  serialize: w('serialize'),
  deserialize: w('deserialize'),
  publish: w('publish'),
  bind: w('bind'),
  inject: w('inject'),
  inspect: w('inspect'),
  compile: w('compile'),
  connect: w('connect'),
  disconnect: w('disconnect'),
  migrate: w('migrate'),
  traverse: w('traverse'),
  subscribe: w('subscribe'),
};

/** Base verb for the *Of convention (claimOf -> "determine the claim"). */
export const DETERMINE_EN = 'determine';
